# Trackly Supabase Setup

Run `company-admin-setup.sql` in the Supabase SQL Editor to enable company workspaces and admin monitoring.

After running the SQL, users create their own company workspace inside Trackly.
The account that creates a workspace becomes that company's admin.
No hard-coded admin email is required.
